---
layout: center
transition: blur-in
---

<ChapterTitle number="06" >
useEffect 的設計問題以及 
<br/>
useEffectEvent 的解決之道
</ChapterTitle>

<!--
上個章節，我們介紹了副作用、純函數，以及 `useEffect` 的真正核心和正確用法，

也提到 `useEffect` 的設計其實在某些場景下是有問題的，

今天這個章節，我就想來討論這個問題，以及該怎麼解決～！
-->

---

# useEffect 的問題

需要和外部系統連接，而且又需要讀取最新的 State 時，就會導致我們需要將 State 加到依賴陣列裡面：

````md magic-move
```jsx
function ChatConnection({ channelId }) {
  const [soundEnabled, setSoundEnabled] = useState(true);

  useEffect(() => {
    const socket = joinChannel(channelId);

    socket.on('message', () => {
      // 當通知音效打開時，播放音效
      if (soundEnabled) {
        playSound();
      }
    });

    return () => socket.leave();
  }, [channelId]);

  return;
}
```

```jsx
function ChatConnection({ channelId }) {
  const [soundEnabled, setSoundEnabled] = useState(true);

  useEffect(() => {
    const socket = joinChannel(channelId);

    socket.on('message', () => {
      // 當通知音效打開時，播放音效
      if (soundEnabled) {
        playSound();
      }
    });

    return () => socket.leave();
  }, [channelId, soundEnabled]); // 要將 soundEnabled 加入依賴陣列

  return;
}
```

```jsx
function ChatConnection({ channelId }) {
  const [soundEnabled, setSoundEnabled] = useState(true);

  useEffect(() => {
    // 每次 soundEnabled 改變，都會導致重新連結聊天室
    const socket = joinChannel(channelId);

    socket.on('message', () => {
      // 當通知音效打開時，播放音效
      if (soundEnabled) {
        playSound();
      }
    });

    return () => socket.leave();
  }, [channelId, soundEnabled]); // 要將 soundEnabled 加入依賴陣列

  return;
}
```
````

<!--
當我們需要和外部系統連接，而且又需要讀取最新的 State 時，就會導致我們需要將 State 加到依賴陣列裡面，

比如以下程式碼，使用者可以透過 `soundEnabled` 決定再收到訊息時，要不要打開音效通知：

[click]
這種場景 React 就會要求你把這個 State 加進依賴陣列裡面，

[click]
但一旦這麼做，Effect 就會在 State 每次變動時重新執行，導致重新連結聊天室。

除了浪費效能以外，整個程式碼的邏輯也很不正確，因為開啟音效不應該重連聊天室。

在過去，我們可以使用 **`useRef`** 去解決這個問題。
-->

---

# useEffectEvent 前的解法 — useRef

使用 `useRef` 建立一個不會改變參考位置的變數：

````md magic-move
```jsx
function ChatConnection({ channelId }) {
  const [soundEnabled, setSoundEnabled] = useState(true);

  useEffect(() => {
    const socket = joinChannel(channelId);

    socket.on('message', () => {
      if (soundEnabled) {
        playSound();
      }
    });

    return () => socket.leave();
  }, [channelId, soundEnabled]);

  return;
}
```

```jsx
function ChatConnection({ channelId }) {
  const [soundEnabled, setSoundEnabled] = useState(true);

  // 建立 ref
  const soundEnabledRef = useRef(soundEnabled);

  useEffect(() => {
    const socket = joinChannel(channelId);

    socket.on('message', () => {
      // 👇 用 ref.current 獲取值
      if (soundEnabledRef.current) {
        playSound();
      }
    });

    return () => socket.leave();
  }, [channelId]); // 👈 不需要將 soundEnabledRef 加入依賴陣列

  return;
}
```

```jsx
function ChatConnection({ channelId }) {
  const [soundEnabled, setSoundEnabled] = useState(true);

  const soundEnabledRef = useRef(soundEnabled);
  // 同步 state 和 ref
  useEffect(() => {
    soundEnabledRef.current = soundEnabled;
  }, [soundEnabled]);

  useEffect(() => {
    const socket = joinChannel(channelId);

    socket.on('message', () => {
      if (soundEnabledRef.current) {
        playSound();
      }
    });

    return () => socket.leave();
  }, [channelId]); // 不需要將 soundEnabledRef 加入依賴陣列

  return;
}
```

```jsx
function ChatConnection({ channelId }) {
  const [soundEnabled, setSoundEnabled] = useState(true);
  const soundEnabledRef = useRef(soundEnabled);

  // 在事件內同步 state 和 ref
  const enableSound = (value) => {
    setSoundEnabled(value);
    soundEnabledRef.current = value;
  };

  useEffect(() => {
    // 省略連結程式碼 ...
  }, [channelId]);

  return (
    <input
      type="checkbox"
      checked={soundEnabled}
      onChange={(e) => enableSound(e.target.checked)}
    />
  );
}
```
````

<!--
我們可以使用 **`useRef`** 建立一個不會改變參考位置的變數，

就像這樣：
[click]

建立一個 Ref 後，用 ref.current 獲取值，這樣也就不需要將其傳入依賴陣列裡面。

但這樣還不夠，我們還需要同步 Ref 和 State，大部分人的解法是使用 useEffect 來同步兩者：

[click]

當 soundEnabled 改變時，就跟著改變 soundEnabledRef：

但經過上個章節，你應該了解了 **`useEffect`** 的本質和目的**：Effect 主要是要和外部系統連結的。**

因此目前這種寫法除了不符合設計目的以外，也讓程式碼變得很難理解、很難閱讀。

更好的做法應該是在事件中去改變 Ref 的值：
[click]

所以我們要用事件去同步 state 和 ref，並在 input 改變時去使用這個事件
-->

---
layout: center
---

# 仍然很難閱讀

難理解 **`soundEnabled`** 和 **`soundEnabledRef`** 的差別。

可能會忘記同步 Ref 和 State

忘記使用 **`.curretn`** 拿到 Ref 的值。

<!--
不過就算這樣，仍然很難閱讀，其他工程師看到程式碼，很難第一時間理解 **`soundEnabled`** 和 **`soundEnabledRef`** 的差別。

況且我們也可能會忘記同步 Ref 和 State，或是忘記使用 **`.curretn`** 拿到 Ref 的值。

因此，React 官方終於意識到這個問題，推出了 **`useEffectEvent`** Hooks 來解決這個問題！
-->

---

# useEffectEvent：現代解法

**`useEffectEvent`** 接收一個函式，並回傳一個**參考位置穩定的事件處理函式**。這個函式在**被呼叫時，永遠會使用最新 Render 中的 State 與 Props。**

````md magic-move
```jsx
function ChatConnection({ channelId }) {
  const [soundEnabled, setSoundEnabled] = useState(true);
  const soundEnabledRef = useRef(soundEnabled);

  // 在事件內同步 state 和 ref
  const enableSound = (value) => {
    setSoundEnabled(value);
    soundEnabledRef.current = value;
  };

  useEffect(() => {
    // 省略連結程式碼 ...
  }, [channelId]);

  return (
    <input
      type="checkbox"
      checked={soundEnabled}
      onChange={(e) => enableSound(e.target.checked)}
    />
  );
}
```

```jsx
function ChatConnection({ channelId }) {
  const [soundEnabled, setSoundEnabled] = useState(true);

  const onMessage = useEffectEvent(() => {
    // 這裡永遠可以安全地讀取最新的 state
    if (soundEnabled) {
      playSound();
    }
  });

  useEffect(() => {
    const socket = joinChannel(channelId);
    socket.on('message', onMessage);
    return () => socket.leave();
  }, [channelId]);

  return;
}
```
````

<!--
**`useEffectEvent`** 接收一個函式，並回傳一個**參考位置穩定的事件處理函式**。

這個函式在被呼叫時，永遠會使用最新 Render 中的 State 與 Props。

所以我們可以將上面的程式碼改成這樣：

[click]

將偵測是不是 soundEnabled 的邏輯，改成用 useEffectEvent 包起來，原本邏輯是在 onMessage 這邊寫的，而我們就只是把它拿到外命並且用 `useEffectEvent` 包起來，

那就像一開始講的，這個函式在被呼叫時，永遠會使用最新 Render 中的 State 與 Props。所以不用擔心拿到舊的 state 問題

而這個 onMessage 是一個穩定的參考值，就像是用 `useCallback` 包起來的函數

如果你還不熟悉 `useCallback`，後面的章節會很詳細的介紹，這邊先記住他是一個穩定的參考值就好。


這裡有一個非常重要的特性：

- **`onMessage`** 不需要放進 dependency array，就像前面說的，因為他是一個穩定的參考值
- Effect 是否重新執行，只取決於 **`channelId`**

這樣除了不需要手動同步 Ref 和 State 以外，也分離的連結系統和事件本身的邏輯。讓程式碼的可讀性大大增加。
-->

---

# useEffectEvent 的原理解釋

```jsx
function useEffectEvent(callback) {
  // 這個 ref 用來保存「最新一次 render 的 callback」
  const latestCallbackRef = useRef(callback);

  // 每次 render 後，自動同步更新 ref
  // 這會在所有 Effect 執行之前完成
  latestCallbackRef.current = callback;

  // 回傳一個參考值穩定的函式
  // 無論被呼叫多少次，都會執行最新的 callback
  const stableWrapper = useCallback((...args) => {
    return latestCallbackRef.current(...args);
  }, []);

  return stableWrapper;
}
```

<!--
其實 **`useEffectEvent`** 不是什麼魔法，它的內部邏輯其實很單純：

照著註解念：

那現實的 **`useEffectEvent`** 會更複雜，因為他需要和 react 的 fiber 架構整合在一起
-->

---

# 總結

React 的 **`useEffectEvent`** 終於解決了一個 **`useEffect`** 長期存在的問題：

**Effect 裡面的事件處理必須要讀取最新的 State，這個 State 會導致 Effect 內部的連結系統重新執行。**

<br/>

<v-click>

過去我們只能透過 **`useRef`** 手動拆解這兩件事

現在如果你的 Effect 需要註冊事件、訂閱外部系統，而這些事件又必須使用最新的 State 時，**`useEffectEvent`** 就會是目前最合適的選擇。

</v-click>

<!--
React 的 **`useEffectEvent`** 終於解決了一個 **`useEffect`** 長期存在的問題：

**Effect 裡面的事件處理必須要讀取最新的 State，這個 State 會導致 Effect 內部的連結系統重新執行。**

[click]

過去我們只能透過 **`useRef`** 手動拆解這兩件事，但代價就是程式碼冗長且語意不清楚，導致容易出錯。

現在如果你的 Effect 需要註冊事件、訂閱外部系統，而這些事件又必須使用最新的 State 時，**`useEffectEvent`** 就會是目前最合適的選擇。

下個章節，我們就要正式進入 React 效能優化的部分了，我們要先從 Re-render 的問題與場景開始，在逐步介紹的相關工具以及記憶化！

那我們就下個章節見囉～！
-->

---
layout: center
---

<Card class="!px-0  whitespace-nowrap">

## 課程驗收

- 說明 useEffect 在什麼場景下的設計是有問題，以及我們要如何改善？

</Card>

<!--
那一樣，我們試著回答這些問題，看看自己有沒有真的了解這個單元的內容

- 說明 useEffect 在什麼場景下的設計是有問題，以及我們要如何改善？
-->

---
layout: center
---

<Card class="">

## 課後行動

- 檢查專案中所有含有訂閱、socket、event listener 的 useEffect，找出 dependency array 因為讀取 state 而導致重新連結的場景。

</Card>

<!--
最後，我們可以嘗試在團隊中應用今天所學的內容，比如：

- 檢查專案中所有含有訂閱、socket、event listener 的 useEffect，找出 dependency array 因為讀取 state 而導致重新連結的場景。
-->
